# /etc/come-to-code.conf
# Intentionally fake, harmless file planted for the live demo.
# If you can read THIS via path traversal, imagine it was /etc/shadow
# or the Kubernetes service account token instead.

[come-to-code-2026]
location = Pignola (PZ)
speaker  = Gabriele Fontana
talk     = "Container at War"
motto    = "more secure" is not "secure"
flag     = CTC{distroless_alone_is_not_enough}
